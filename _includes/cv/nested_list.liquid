<ul class="card-text font-weight-light list-group list-group-flush">
  {% for content in entry.contents %}
    <li class="list-group-item">
      <h5 class="font-italic">{{ content.title }}</h5>
      {% if content.items %}
        <ul class="subitems">
          {% for subitem in content.items %}
            {% if subitem.items %}
              <h6 class="font-italic">{{ subitem.title }}</h6>
              <ul class="subsubitems">
                {% for subsubitem in subitem.items %}
                  <li>
                    <div class="subsubitem cv-markdown">{{ subsubitem | markdownify }}</div>
                  </li>
                {% endfor %}
              </ul>
            {% else %}
              <li>
                <div class="subitem cv-markdown">{{ subitem | markdownify }}</div>
              </li>
            {% endif %}
          {% endfor %}
        </ul>
      {% endif %}
    </li>
  {% endfor %}
</ul>
